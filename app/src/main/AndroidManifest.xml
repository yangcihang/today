<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.hrsoft.today">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mvp.view.main.activity.MainActivity">
            <!--<intent-filter>-->
            <!--<action android:name="android.intent.action.MAIN"/>-->

            <!--<category android:name="android.intent.category.LAUNCHER"/>-->
            <!--</intent-filter>-->
        </activity>
        <activity android:name=".mvp.view.square.activity.SquareActivity"/>
        <activity android:name=".mvp.view.square.activity.SearchActivity"/>
        <activity android:name=".mvp.view.manage.activity.ManageCalendarActivity"/>
        <activity android:name=".mvp.view.manage.activity.CreateCalendarActivity"/>
        <activity android:name=".mvp.view.detail.activity.CalendarDetailActivity"/>
        <activity android:name=".mvp.view.account.activity.AccountActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>